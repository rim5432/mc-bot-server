# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run
runs
run-data
repo

# stray wrapper extraction artifact at repo root only
/META-INF/

# tool runtime artifacts (locks, logs)
tool/.runtime/

# external harness runtime scratch
.dsh-runtime/

# local agent-session scratch (plans, workspace state)
.zcode/
