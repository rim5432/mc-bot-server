// SanityCheck was a one-off debug file used to verify STUCK
// timing in LimboCharacterizationGateTest.restFiresStuckViaMotionFuse.
// The actual rig sanity now lives in that test; this file is
// intentionally empty to keep the directory listing clean.
// Kept (not deleted) only because the local safety policy blocks
// direct Remove-Item on Windows; the file has no executable
// content so it is harmless to the build.
