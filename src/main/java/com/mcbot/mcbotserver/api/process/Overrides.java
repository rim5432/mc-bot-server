package com.mcbot.mcbotserver.api.process;

/**
 * Behavior-layer parameter overrides attached to a directive. Stage 0
 * placeholder — the frozen contract names the field, Stage 1 gives it
 * its first real component (e.g. sprint allowance).
 *
 * <p>Contract: see ADR-0002 section 1 (Directive{Goal, Overrides}).
 */
public record Overrides() {
}
